// Database stub - database functionality has been removed
// This file is kept to prevent import errors
import { createDbConnection } from '@/utils/DBConnection';

const db = createDbConnection();

export { db };
